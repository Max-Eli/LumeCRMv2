# Environment / secrets
.env
.env.local
.env.*.local
*.pem
*.key

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Django
*.log
db.sqlite3
db.sqlite3-journal
/staticfiles/
/media/
local_settings.py

# Node / Next.js
node_modules/
.next/
out/
build/
dist/
.npm/
.pnp.*
.yarn/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude Code per-project state (NOT shared across collaborators)
.claude/

# Screenshots / scratch files at the repo root
Screenshot*.png
Screenshot*.jpg

# CSV / data exports at the repo root. Vendor migrations (Zenoti,
# Vagaro, etc.) drop guest lists + appointment exports here for the
# importer to read; these contain real customer PII and PHI and MUST
# NEVER land in git history. Excluded across the repo, not just root,
# because a misplaced export anywhere is the same incident.
*.csv
*.xlsx
*.xls
